## 最终结论：**不放行**

### P1-1：失败重新生成一致性——通过

已确认：

- 异常时同一事务恢复旧正文和旧 `response_source_request_id`。
- 前端收到 `error` 后恢复旧正文，复制读取恢复后的内容。
- 消息 API、JSON/TXT/Markdown 导出统一读取相同可见正文。
- 临时数据库断言验证了旧来源恢复、失败请求独立标记为 `failed`。

### P1-2：并发重新生成互斥——未通过

`backend/app/api/workspace.py:1253-1259` 的活动根请求检查是普通查询。在 MySQL/InnoDB 默认 `REPEATABLE READ` 下，并发事务可能基于获取行锁前建立的旧快照，漏掉等待期间新提交的活动请求，进而创建第二个根请求。

现有测试只覆盖“首个根请求已提交后再请求”的场景，且 SQLite 不验证真实 `FOR UPDATE` 语义。

建议将活动请求检查改为当前读，并增加 MySQL 双 Session、首提交前同时进入的并发回归。

### 验证结果

- 后端：`138 passed`
- 前端：`39 passed`
- 生产构建：通过
- 针对性前后端测试：通过

完整报告：

[review-AI回复展示优化-20260903-v4.md](./md/review-AI回复展示优化-20260903-v4.md)
t_id`，随后释放行锁。
4. B 获取 assistant 行锁后，`backend/app/api/workspace.py:1253-1259` 使用未加锁的普通 `SELECT` 检查活动请求；在 `REPEATABLE READ` 的旧快照中可能看不到 A 刚提交的请求。
5. B 因此还能创建第二个 `running` 根请求。两个生成器可继续交错写同一 assistant，最终正文与来源绑定仍可能错配。

进程内 `idempotency_lock` 也未消除此窗口：它只覆盖请求准备阶段，且 B 在进入该锁前已经执行数据库读取；它也不覆盖多 worker/多实例。不同重新生成使用不同 Idempotency-Key，现有唯一索引不会冲突。

现有并发测试是在第一个 Provider 已经阻塞后才发起第二个 HTTP 请求。此时第一个根请求早已提交，所以只证明“已提交活动请求可返回 409”，没有覆盖“两个事务都在首个提交前进入”的临界顺序；测试数据库还是 SQLite，`FOR UPDATE` 不提供生产 MySQL 的行锁语义。

**修复验收要求**

- assistant 行锁之后对活动根请求执行当前读，例如给该查询增加 `FOR UPDATE`，或把活动请求 ID 放入受同一行锁保护的显式字段并以原子条件更新。
- 保持“锁 assistant → 当前读检查活动请求 → 创建根请求 → 清空正文并绑定来源 → 单次提交”的事务边界。
- 新增两个独立 Session/worker 的数据库并发回归：让 B 在 A 提交前建立事务并等待 assistant 行锁；A 提交后，B 必须读到活动根请求并返回 409。该回归应在 MySQL/InnoDB 上执行，SQLite 用例只能保留为接口层补充验证。

## 2. 第三轮 P1 验收结果

| 验收项 | 结果 | 依据 |
|---|---|---|
| 失败重新生成恢复旧正文 | 通过 | `workspace.py:1060-1071` 在异常分支将 `stream_assistant.content` 恢复为 `restore_content`，并与失败请求状态同一次提交。 |
| 失败重新生成恢复旧来源 | 通过 | 重新生成前复制旧 `content_json`（`workspace.py:1263-1264`），失败时原样回写（`workspace.py:1067-1068`）。临时加强断言验证：消息重新指向旧官网根请求，失败 Codex 根请求独立保留为 `failed`。 |
| 页面与复制一致 | 通过 | 前端 `error` 事件恢复 `restoreContent`（`src/App.tsx:349-359`），复制直接读取恢复后的 `message.content`（`src/App.tsx:751-758`）；永久前端回归通过。 |
| API/JSON/TXT/Markdown 一致 | 通过 | 消息 API 与三种导出统一走 `_visible_message_content()`；失败路径回归逐一验证三种格式均为旧正文。 |
| 同一 assistant 的已活动请求互斥 | 通过（有限场景） | 当前测试验证首个根请求已提交且 Provider 阻塞时，第二次重新生成返回 409。 |
| 同一 assistant 的真正并发原子互斥 | **未通过** | 活动根请求检查不是当前读，且缺少首提交前双事务竞态回归；行锁与原子创建尚未形成可证明的完整互斥。 |

## 3. 本次验证记录

- 针对性后端回归：`1 passed`。
- 临时加强的失败回滚数据库断言：`1 passed`；已确认旧正文、旧来源请求 ID、失败根请求状态及失败渠道，临时改动已还原。
- 针对性前端失败重新生成回归：`1 passed`。
- 后端全量：`138 passed`。
- 前端全量：`39 passed`（7 个测试文件）。
- 生产构建：通过；产物为 `dist/assets/index-DnFUF17m.js`、`dist/assets/index-DQQXg4pf.css`。
- 本机 MySQL 服务未运行，因此本轮未执行 MySQL 双事务复现；静态事务/隔离级别审计已确认上述窗口，SQLite 绿色结果不足以覆盖该语义。

## 4. 最终结论

第三轮 P1-1 已关闭：失败重新生成会恢复旧正文和旧来源，页面、复制、刷新后的消息 API 以及 JSON/TXT/Markdown 导出保持一致。

第三轮 P1-2 仅关闭“活动请求已经提交后再发起冲突请求”的顺序场景，首提交前真正并发进入时仍存在旧快照漏检窗口。当前版本 **不予最终放行**；完成活动请求当前读/原子 guard 修复并补齐 MySQL 双事务回归后，再执行放行复核。
