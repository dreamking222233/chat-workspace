当前：代码审查已完成；结论为 **需修改后通过**。

## 主要发现

### [P2] `response_options` 可绕过思考等级枚举校验

涉及位置：

- `backend/app/api/workspace.py:1055-1062`
- `backend/app/api/workspace.py:1166-1172`
- `backend/app/services/providers.py:127-130`
- `backend/app/services/providers.py:189-192`

`response_options` 会作为 `extra` 最后合并到 Provider 请求，可覆盖已经 Schema 校验的：

- Chat Completions：`reasoning_effort`
- Responses：`reasoning`

已实际复现：Schema 中传入合法 `low`，最终 Provider payload 可被覆盖为无效等级，可能导致上游 4xx。

建议禁止 `extra` 覆盖 `reasoning_effort`、`reasoning`，并增加两个 Provider 回归测试。

## 其他建议

1. 前端测试目前只真正发送并断言了 `xhigh`，建议参数化覆盖四档和关闭状态。
2. 补充重新生成请求的 `reasoning_effort` 断言。
3. 思考菜单增加点击外部关闭、Escape、方向键及焦点回收。
4. 同步 Plan 中构建与 Review 的完成状态。

## 验证结果

- 前端测试：31 项通过
- 后端测试：128 项通过
- 生产构建：通过
- Review 文档：[review-聊天思考等级-20260902.md](./md/review-聊天思考等级-20260902.md)
g` 覆盖已生成的结构。因此 API 客户端仍可让任意等级进入上游请求，既违背实施方案中的枚举约束，也可能造成上游 4xx。

已复现：直接字段传入 `low`，同时令 `response_options` 带无效等级，最终 Chat 与 Responses Provider payload 均得到无效等级。

建议：在两个 payload builder 的 `extra` 保留键集合中增加 `reasoning_effort` 和 `reasoning`，并明确核心强类型字段优先于扩展字段；或者先合并扩展字段，再由强类型字段覆盖。补充两个回归测试，分别覆盖 Chat Completions 和 Responses。

## 3. 优化建议

1. **补齐四档及关闭态的前端参数化测试。** `src/App.test.tsx:103-137` 的用例名称声称覆盖 low 到 xhigh，实际只发送并断言了 xhigh。建议参数化验证 `low`、`medium`、`high`、`xhigh`，并验证选择“关闭思考”后请求体不再含 `reasoning_effort`。
2. **补充重新生成链路断言。** 当前重新生成测试仅检查模型与渠道解析，未验证所选 `reasoning_effort` 是否进入 `/regenerate` 请求及 Provider。增加接口级捕获测试可防止 UI、Schema、Workspace、Provider 任一层断链。
3. **完善菜单交互。** 思考等级菜单目前只通过再次点击按钮或选择项目关闭；建议支持点击外部区域、`Escape`、方向键导航和焦点回收，以匹配 `menu/menuitemradio` 语义。
4. **同步方案状态。** Plan 的实施步骤仍将“构建、Review 与文档更新”合并为未完成项，而 Impl 已声明构建通过；建议拆分并分别标记状态，避免审查状态与构建状态互相覆盖。

## 4. 验证结果

- 前端：`npm test -- --run`，7 个测试文件、31 项测试通过。
- 构建：`npm run build` 通过。
- 后端：`cd backend && pytest -q`，128 项测试通过；存在 134 条第三方弃用警告，与本功能无直接关系。
- 附加复现：确认 `response_options` 可覆盖 Chat Completions 的 `reasoning_effort` 与 Responses 的 `reasoning`。

## 5. 建议验收条件

- 修复扩展字段覆盖强类型思考等级的问题。
- 增加对应 Provider 回归测试。
- 前端至少补充关闭态与重新生成参数断言。
- 完成上述项目后，可将 Review 结论更新为“通过”。
