# 文本模型兼容工具调用第二轮代码审查

日期：2026-09-02

## 审查范围

- `md/plan-文本模型兼容工具调用-20260902.md`
- `md/impl-文本模型兼容工具调用-20260902.md`
- `backend/app/services/platform_tools.py`
- `backend/app/schemas/common.py`
- `backend/app/api/workspace.py`
- `backend/tests/test_platform_tools.py`
- `backend/tests/test_api.py`

## 最终分级结论

**通过（A，无阻断问题）。**

核心实现符合方案：原生 `tool_calls` 保持优先；无原生调用时解析平台工具标签并复用
现有图片工具状态机；平台调用结果通过兼容消息回放完成第二轮答复；参考图资产 ID 可
传入编辑参数并继续执行服务端归属与文件校验。

## 第一轮问题复核

### 超长标签缓冲：已修复

- 未闭合标签在增量追加时执行 `MAX_PLATFORM_TOOL_PAYLOAD_CHARS` 上限检查。
- 超限内容立即作为普通文本释放，并清空 `call_buffer`、`pending` 和 `in_call`。
- 已闭合但超长的标签在 JSON 解析前拒绝，不生成工具调用。
- 回归覆盖未闭合和已闭合两种超限路径。

### 额外参数：已修复

- `GenerateImageToolArguments` 已设置 `ConfigDict(extra="forbid")`。
- 行为与函数 Schema 的 `additionalProperties: false` 一致。
- 未知参数会触发 `ValidationError`，进入既有失败工具结果流程。

## 回归检查

- 跨 SSE chunk 标签解析、普通文本流、非法/不完整标签、多标签均有单元测试。
- 只对 `platform-` ID 的调用降级回放；原生 OpenAI 工具消息结构保持不变。
- 端到端测试覆盖附件元数据、参考图编辑、工具和图片 SSE 事件、第二轮结果回放，且
  平台协议标签未进入用户可见 SSE 或 assistant 正文。

## 验证结果

- `python -m compileall -q app tests`：通过。
- `python -m pytest -q`：**33 passed**。
- `npm run build`：通过。
- 真实 OpenAI-compatible 渠道两轮 SSE：第一轮平台调用参数通过校验；第二轮输出最终
  确认且没有重复调用。

## 结论

第一轮审查问题均已修复并有回归覆盖，未发现本次修复引入的新回归。**审查通过。**
