# 思考等级滑块 Review

- 功能名称：effort-slider
- 日期：20260905
- Review 轮次：1

## 结论

通过。请求契约未改，交互从菜单换成滑块后，档位、禁用、关闭路径仍与原测试覆盖一致。

## 核对

| 项 | 结果 |
| --- | --- |
| 五档仍映射 `null/low/medium/high/xhigh` | 通过 |
| 拖拽吸附与键盘调档 | 通过（组件测试 + Chrome） |
| 图片模式禁用且不展开 | 通过（`disabled={imageMode}`） |
| 打开模型选择会关掉滑块 | 通过（原 `onOpenChange` 逻辑保留） |
| 关闭思考不发送 `reasoning_effort` | 通过（App 测试） |
| 窄屏卡片不裁切 | 通过（390px 截图） |

## 残留

- 官方截图标题是「模型名 + Ultra」，本页模型选择仍在右侧独立控件，卡片标题用「思考 + 档位」避免和模型列表重复。
- 420px 宽度下组合区较挤，占位符可能折行；该宽度隐藏图片按钮以保留思考入口。
