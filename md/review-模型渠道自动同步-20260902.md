审查通过。

- 三请求 Condition 屏障复现：`provider_calls=1`、两个等待者均复用结果，完成窗口竞态已关闭。
- 后端全量回归：`36 passed`（隔离 SQLite）。
- 前端回归：`3 passed`；生产构建与 Python 编译通过。
- StrictMode、60 秒清理、前后端重入、URL/密钥及失败路径均已复核。

审查记录已归档：`md/review-模型渠道自动同步-20260902.md`
age.test.tsx`

部署前提：单 Uvicorn worker；不评估多实例分布式互斥。

## 审查结论

**审查通过。** 未发现阻塞项。

## 重点复核结果

1. **三请求完成窗口竞态已关闭**
   - `sync_models` 在同一 `Condition` 临界区内保持 `active=True`，完成后原子地
     写入 `last_result`、递增 `generation`、清除 `active` 并 `notify_all()`。
   - 等待请求记录进入时的 generation，只在观察到新 generation 且有结果时复用该结果；
     因此两个已在飞行中的跟随请求不会在 owner 完成后重新取得 owner 身份。
   - 以三个并发调用的独立屏障复现验证：先阻塞 owner，确认另外两个请求均已进入
     `Condition.wait()` 后才释放 owner。结果为
     `provider_calls=1`、`condition_waiters=2`、`reused_results=2`。
   - 项目回归 `test_admin_channel_concurrent_sync_hits_provider_once` 也覆盖三个 HTTP
     请求、一次 Provider 调用和两个“模型已由并发请求同步”响应。

2. **StrictMode、60 秒轮询与前端重入**
   - 首次轮询由 `setTimeout(0)` 延迟，effect cleanup 会清掉预演定时器；后续使用
     `setInterval(60000)`。
   - cleanup 同时清除首次定时器、interval，并中止未完成的自动请求。
   - 组件级 `syncTasksRef: Map<channelId, Promise>` 让自动同步、手动同步和快速重复
     触发共享同一个 Promise；`pollInFlightRef` 防止轮询重叠。
   - Vitest 已验证 StrictMode 下首次仅一次同步、60 秒再次执行、卸载后停止，以及
     手动操作复用渠道锁。

3. **URL、密钥与失败路径**
   - Create/Update schema 和 `api_base_url` 均拒绝带 userinfo 的 Base URL。
   - Base URL Origin 改变且未提交新密钥时返回 422；原渠道 URL 与掩码密钥保持不变。
   - 远端模型请求失败时保留已有模型，记录 `last_sync_error`，并返回结构化失败结果；
     并发跟随者复用失败结果而非再次访问 Provider。

4. **回归与构建验证**
   - 隔离 SQLite：目标后端回归 `3 passed`。
   - 隔离 SQLite：全量后端 `python -m pytest -q`：`36 passed`。
   - 前端 `npm test -- --reporter=verbose`：`3 passed`。
   - `npm run build`：通过。
   - `python -m compileall -q app tests`：通过。

## 非阻塞说明

- 渠道同步状态保存在当前进程内存，符合单 Uvicorn worker 的既定部署边界；多实例场景需
  另行引入共享锁或任务协调机制，不属于本轮范围。
