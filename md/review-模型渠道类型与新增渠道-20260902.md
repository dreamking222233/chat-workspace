# 模型渠道类型与新增渠道代码审查

- 审查日期：2026-09-02
- 对照文档：`md/plan-模型渠道类型与新增渠道-20260902.md`、`md/impl-模型渠道类型与新增渠道-20260902.md`

## 审查结论

**通过。** 渠道类型字段在 ORM、迁移、Pydantic 契约、管理表单和列表展示中保持一致；旧数据库通过启动兼容逻辑与 Alembic `0004_channel_type` 均可升级，历史渠道默认 `official`。两类渠道继续共用 OpenAI 协议适配器，类型作为可见配置元数据，不会改变请求格式。

## 检查项

- API 创建/更新/列表返回 `channel_type`，仅接受 `official` / `codex`。
- 管理页新建默认官网版，编辑时回填真实类型，列表徽目标识“官网版”或“Codex GPT”。
- 8080 渠道模型列表来自真实 `/v1/models`，13 个模型按名称映射文本/图片能力。
- `/v1/chat/completions` 流式文本请求 HTTP 200；图片端点实际返回上游 HTTP 403，并已在实施记录中注明权限状态，未伪造生成成功。
- 前端 29 项测试、后端 126 项测试、生产构建全部通过。
- 当前 MySQL Alembic 版本为 `0004_channel_type`，运行 API 中存在 1 个官网版渠道和 1 个 Codex GPT 渠道。

## 后续运维

图片 Key 的上游分组开通图片生成权限后，使用管理页“测试连接”及聊天图片模式复测；自动同步每 60 秒继续读取 `/v1/models`。
