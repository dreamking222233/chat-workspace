# 聊天页输入框、权限提示与模型选择最终审查

日期：2026-08-28

## 审查结论

通过。输入文字已与胶囊输入框垂直居中；未授权提示改为警告条；模型选择在自定义组件中渲染，Windows 不再依赖无样式原生 `select`；未授权点击不会列出模型。

## 已覆盖需求

- 底部输入框文字上下居中。
- 「当前账户尚未开通使用权限，请联系管理员。」足够醒目。
- 封装自定义 Select，跨系统外观一致。
- 未授权不展示模型名单。

## 验证结果

| 项目 | 结果 |
| --- | --- |
| TypeScript | 通过 |
| Vitest SelectMenu + App | 23 passed |
| textarea 与 composer 中心差 | 0 |
| 上下 padding | 均为 9px |
| 原生 `select.model-select` | 不存在 |
| 已授权下拉 | listbox 打开，17 个 option |
| 未授权横幅 | 13px / 600 / 红底 |
| 未授权点击 | listbox=0，option=0 |

## 残余风险

- 多行输入时按钮仍相对整块 textarea 垂直居中，而不是贴底；单行是本次主要场景。
- 自定义面板最大高度 280px，模型很多时需滚动。
- 未授权触发器仍显示「自动模型」，不展示具体模型名。
