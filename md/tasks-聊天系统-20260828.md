# ChatGPT 对话系统详细 Tasks

日期：2026-08-28  
执行基线：`md/plan-聊天系统-20260828.md` v2

## 已完成（本轮）

- [x] T01 建立 `backend/app` Python 包和 FastAPI 应用入口。
- [x] T02 增加环境配置、CORS、SQLite 开发数据库和健康检查。
- [x] T03 增加 `users`、`entitlements`、`model_channels` SQLAlchemy 模型。
- [x] T04 增加注册/登录接口和管理员角色依赖。
- [x] T05 增加模型渠道 CRUD、启停、删除和连通性检查接口。
- [x] T06 增加渠道输入校验：模态、URL、优先级、模型列表和重复名称。
- [x] T07 增加 API Key 掩码响应，服务端不向列表返回完整密钥。
- [x] T08 在现有聊天账户菜单挂载“管理端：模型渠道”。
- [x] T09 增加渠道管理页：筛选、表格、创建/编辑、启停、测试、删除确认和移动端布局。
- [x] T10 完成前端生产构建和 Python 语法检查。
- [x] T10.1 增加用户端 Landing/Login/Register 路由和身份状态。
- [x] T10.2 将管理端拆为独立 `/admin/*` 布局，按管理员角色保护路由。
- [x] T10.3 增加管理员 `/auth/me` 身份接口和用户端/管理端返回入口。

## 下一批执行顺序

### 认证与权限

- [x] T11 把 refresh token、撤销时间和设备信息加入持久化模型。
- [x] T12 增加授权期限 Service，并在发送文本/图片请求前校验有效期。
- [x] T13 增加登录页、注册页、开始使用页和受保护路由。
- [x] T14 将前端 `Dream` 账户区替换为当前登录用户资料。

### 项目与对话

- [x] T15 增加 `projects`、`threads`、`messages`、`assets`、`model_requests` 表和迁移。
- [x] T16 实现项目/会话/消息 CRUD、归档、恢复、重命名和用户隔离。
- [x] T17 把 `App.tsx` 的本地线程状态迁移到 API hooks。
- [x] T18 实现导出 JSON/Markdown/TXT 和短期下载链接。

### 模型调用

- [x] T19 实现 OpenAI-compatible 文本 Provider，按渠道优先级选择启用渠道。
- [x] T20 实现 SSE 事件 ID、断线重连、停止生成和重复事件去重。
- [x] T21 实现图片 Provider、任务状态、图片资源保存和下载。
- [x] T22 将真实请求 usage 回写 `model_requests`，token 为空时保留 NULL。
- [x] T23 在聊天输入器增加模型/模态选择，并根据渠道可用模型动态展示。

### 管理端与验证

- [x] T24 实现管理端用户列表、权限授权弹窗和审计日志。
- [x] T25 实现模型使用记录页面和筛选、分页、导出。
- [x] T26 为管理员权限、用户越权、渠道校验、授权过期编写 pytest。
- [x] T27 增加 Docker Compose、Alembic 迁移、备份和部署说明。
- [x] T28 完成前端构建、后端测试和 MySQL 结构验收；视觉回归待可用浏览器环境复核。

## 完成说明

T01-T27 已实现并通过本地 MySQL 集成验证；T28 的自动化构建与后端测试已通过，像素级视觉回归依赖浏览器采集环境，当前仅完成源码与响应式样式检查。

## 收尾修复（2026-08-28）

- [x] T29 修复 SSE 生成器的请求级数据库会话生命周期，确保 assistant 正文、请求状态和 token 在流结束后持久化。
- [x] T30 重新生成改用独立 `/threads/{thread_id}/regenerate` 流程，复用原 assistant 消息，不重复写入用户提示词。
- [x] T31 指定文本/图片模型没有启用渠道时返回明确 503，避免静默回退本地演示模型；重新生成纳入模型限流。
- [x] T32 禁止会话切换到已归档项目，用户端刷新后加载已归档对话并可恢复。
- [x] T33 修复 HTTP 模型错误的乐观消息回滚、项目对话计数刷新和管理端模态筛选清理。
- [x] T34 新增并通过 SSE 持久化、重新生成去重、缺失渠道和归档项目回归测试；完成运行中 MySQL 链路验证。
